# Default-deny the normal repository context. The development-environment test
# builds a separate context from an explicit Git-tracked allowlist.
*

!Dockerfile
!resolve_python.sh
!build_uv_docker.sh
!setup_rocm_venv.sh
